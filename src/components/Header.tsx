type Props = {};

const Header = (props: Props) => {
  return (
    <div className="w-full bg-blue-600 px-[20px] py-[20px] h-[72px] flex items-center">
      Header
    </div>
  );
};

export default Header;
