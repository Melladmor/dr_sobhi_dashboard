import React from "react";

type Props = {};

const SideBar = (props: Props) => {
  return (
    <div className="bg-black text-white w-[15%] h-screen px-[30px]">
      SideBar
    </div>
  );
};

export default SideBar;
